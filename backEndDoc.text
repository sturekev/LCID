
Setup Environment:
    python3 -m venv venv

install requirements:
    pip install -r BackEndRequirements.txt

run local for fastapi:
    cd Backend
    uvicorn main:app --reload

For database:
    python -m pip install --upgrade pip 
    pip install psycopg2
    *This version is 2.9.3